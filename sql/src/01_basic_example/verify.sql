SELECT
     :id as `id`
     ,:bool as `bool or boolean`
     ,:datetime as `datetime`
     ,:date as `date`
     ,:time as `time`
     ,:array as `array`
     ,:filename as `filename`
     ,:token as `token`
     ,:module as `module`
     ,:key as `key`
     ,:number as `number`
     ,:mime as `mime`
     ,:string as `string`
     ,:json as `json`
     ,:url as `url`
     ,:email as `email`
     ,:html as `html`
